## 🚀 How to Run

### Server

1. Run `monitor.exe`.
2. Keep the program running.

### Web Interface

1. Open `index.html` in your browser.
2. Click **Connect**.
3. Enter the IP address of the PC running `monitor.exe`.

> Port `8000` is added automatically.
> Both devices must be connected to the same local network.
